{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm index",
    "start:test": "dotenv -e .env.test.local  nodemon index",
    "start:dev": "dotenv -e .env.development.local  nodemon index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.0",
    "dotenv-cli": "^5.1.0",
    "express": "^4.17.1",
    "http-proxy-middleware": "^2.0.6"
  }
}
